# Environment Configuration
NODE_ENV=development
PORT=3000

# Database
MONGODB_URI=mongodb://localhost:27017/fleetman

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-make-it-at-least-32-characters-long
JWT_EXPIRES_IN=24h

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ================================
# Email Configuration - Sprint #15
# ================================

# SMTP Settings
# -------------
# Development: Use Mailtrap (https://mailtrap.io)
# 1. Sign up for free account
# 2. Go to "Email Testing" -> "Inboxes" -> Select inbox
# 3. Copy SMTP credentials from "SMTP Settings"

SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_mailtrap_username_here
SMTP_PASS=your_mailtrap_password_here

# Email Settings
EMAIL_FROM=noreply@fleetman.com
APP_BASE_URL=http://localhost:5173

# Production SMTP Providers (Examples):
# -------------------------------------
# SendGrid (100 emails/day free):
#   SMTP_HOST=smtp.sendgrid.net
#   SMTP_USER=apikey
#   SMTP_PASS=your_sendgrid_api_key
#
# Postmark (100 emails/month free):
#   SMTP_HOST=smtp.postmarkapp.com
#   SMTP_USER=your_postmark_server_token
#   SMTP_PASS=your_postmark_server_token
#
# Mailgun (5,000 emails/month for 3 months):
#   SMTP_HOST=smtp.mailgun.org
#   SMTP_USER=postmaster@your-domain.mailgun.org
#   SMTP_PASS=your_mailgun_smtp_password

# Cronjob Configuration - Sprint #11: Maintenance Alarms
# Cron expression for maintenance tasks (update hours + check alarms)
# Format: minute hour day month weekday
# Examples:
#   - Testing (every 5 minutes): */5 * * * *
#   - Production (daily at 2am UY time): 0 5 * * *
#   - Every 2 hours: 0 */2 * * *
CRON_MAINTENANCE_SCHEDULE=0 5 * * *